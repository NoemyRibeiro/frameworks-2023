1. instalar o NodeJS (noejs.org)
2. npm --version (check do nodejs instalado)
3. instalar o angular (https://angular.io/guide)
   3.1 npm install -g @angular/cli
4. ng version (check angular instalado)



# _____________ Rotinas go GIT
1. git status (verificar situação)
2. se vermelho -> git add .
3. se verde -> git commit -m "nome do commit"
4. se td ok! git push (atualiz no github)

5. git clone endereço.git . (comando para copiar do github p local)
5.1 npm install (dentro da pasta do projeto p instalar node_modules)

_______________ PARA REMOVER USUÁRIO ANTERIOR _____________________

git config --system 

#_____________ ANGULAR (DENTRO DA PASTA DO PROJETO)
ng new nome-projeto (criar um projeto)

ng serve -o (rodar e abrir o projeto - precisa estar dentro da pasta do projeto)
